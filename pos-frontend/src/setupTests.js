/* The line `import '@testing-library/jest-dom';` is importing the Jest DOM library from the
testing-library package. This library provides custom Jest matchers for asserting on DOM elements.
It enhances the testing experience by providing additional matchers and utilities specifically
designed for testing DOM elements in Jest tests. */

import '@testing-library/jest-dom';
